package-lock.json
public/swagger/*
node_modules
.vscode
dist

# Generated by `npm run schema:generate`, which normalises its own formatting.
# Prettier collapses short arrays at printWidth 140 while the generator always
# expands them, so leaving these in would make `npm run format` and the generator
# fight over the files on every run.
public/schema/openapi.json
public/schema/openapi.v1.json
public/schema/openapi.v2.json